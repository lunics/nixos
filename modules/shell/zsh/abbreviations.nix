{
  programs.zsh.zsh-abbr = {
    enable = true;
    # package = pkgs.zsh-abbr;
    abbreviations = {
      # gco = "git checkout";
      # l = "less";
    };
    globalAbbreviations = {
      # G = "| grep";
      # L = "| less -R";
    };
  };
}
